import React  from 'react';

const Header = () => {
    return (
        <header className="header">
            <div className="logotype">VK Clone</div>
        </header>
    );
};

export default Header;
